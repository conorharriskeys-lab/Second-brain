# Scripts

Small deterministic utilities belong here.

Rules:

- safe by default
- explicit input/output
- dry-run where mutations are possible
- no secrets in source
- avoid scripts that silently perform bulk destructive changes
