# Dashboard

Future local interface for the second brain.

Do not build a large application until the underlying capture → organization → retrieval pipeline is proven.

The dashboard should consume the knowledge layer rather than become the canonical data store.
